<div class="container">
  <%= form_with(model: @category, class: "shadow p-3 mb-3 bg-info rounded") do |f| %>
    <% if @category.errors.any? %><div class="alert alert-danger alert-dismissible fade show" role="alert">
    <h4 class="alert-heading">The following errors prevented the article from being saved</h4>
    
     <div class="alert alert-danger alert-dismissible fade show" role="alert">
    <h4 class="alert-heading">The following  prevented the article from being saved</h4>
      <div id="error_explanation">
        <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

        <ul>
          <% @category.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="form-group row">
    <%= f.label :name, "Category name", class: "col-2 col-form-label text-light" %>
     <div class ="col-10">
    <%= f.text_field :name ,class:"form-control shadow rounded",placeholder: "Enter a name"%>
  </div>
</div>

  
 <div class="form-group text-center">
  <div class="d-flex justify-content-center">
    <%= f.submit class: "btn btn-warning btn-lg" %>
  </div>
</div>
<% end %>
</div>